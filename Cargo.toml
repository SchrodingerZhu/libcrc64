[package]
name = "libcrc64"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crc64fast = { version = "1.0.0", features = ["pmull"] }

[lib]
name = "crc64"
crate-type = ["staticlib"]
